define("tool_flowboard/canvas/strings",["exports","core/str"],(function(_exports,_str){Object.defineProperty(_exports,"__esModule",{value:!0}),_exports.str=_exports.preload=void 0;
/**
   * Every lang string the node types themselves named — a field's label, an
   * option's own text — resolved once, so the inspector can read them back
   * synchronously while it draws.
   *
   * @module     tool_flowboard/canvas/strings
   * @copyright  2026 Didactika.org
   * @author     Hector Arrechea <hectorlazaroarrechea@gmail.com>
   * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
   */
const resolved=new Map,ALWAYS_NEEDED=["flow:error_required"];_exports.preload=async nodeTypes=>{const keys=(nodeTypes=>{const keys=new Set(ALWAYS_NEEDED);return Object.values(nodeTypes).forEach((meta=>{(meta.config||[]).forEach((field=>{keys.add(field.label),Object.values(field.options||{}).forEach((optionLabel=>keys.add(optionLabel)))}))})),Array.from(keys)})(nodeTypes);if(0===keys.length)return;const values=await(0,_str.getStrings)(keys.map((key=>({key:key,component:"tool_flowboard"}))));keys.forEach(((key,index)=>resolved.set(key,values[index])))};_exports.str=key=>resolved.get(key)||key}));

//# sourceMappingURL=strings.min.js.map